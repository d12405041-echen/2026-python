# AI_USAGE.md - Week 09 模板開發紀錄

## 🤖 AI 協作紀錄

### 1. 我問 AI 的 3 個問題
*   在 Python 中，`heapq` 模組如何自定義類別的比較邏輯（`__lt__`）？
*   如何優雅地實作 Trie 字典樹的遞迴插入？
*   哈夫曼編碼中，如何確保頻率相同時，樹的合併順序是一致的？

### 2. 我採用的建議
*   **`heapq` 比較運算**：採用 AI 建議在 `HuffmanNode` 實作 `__lt__` 的指紋，這比手寫比較函數更符合 Pythonic 標準。
*   **遞迴生成編碼**：採用了 AI 提供的深度優先遍歷邏輯來生成哈夫曼編碼表。

### 3. 我拒絕的建議
*   **拒絕使用第三方套件**：AI 一開始推薦用 `pyahocorasick` 等套件。但我堅持「自主實作」，因為 August 聖經要求的是手寫模板以理解底層。
*   **拒絕過度複雜的二進位處理**：針對本週練習，AI 建議了一套處理 bit manipulation 的複雜邏輯，我決定簡化為字串 0/1 表示，保持代碼在範例階段的清晰。

### 4. AI 錯誤修正案例
*   **Trie 插入 Bug**：AI 給的簡單 Trie 範例漏掉了 `is_end` 標記。
*   **手動修正**：我補上了結束標記，這在處理「前綴重複」（如 apple vs app）的情況下至關重要。

---
**學號**：1112405041 | **姓名**：李易宸
