# AI_USAGE.md - XOR 複合函數與大老二引擎偵破紀錄

## 🤖 AI 協作紀錄

### 1. 我問 AI 的 3 個問題
*   如何判斷多個單調函數複合後的增減性？
*   在實作大老二卡牌模型時，為什麼 `Card` 物件必須實作 `__hash__` 才能放進 `set`？
*   Python 的 `float` 四捨五入到第四位時，如何避免常見的精度誤差？

### 2. 我採用的建議
*   **XOR 邏輯**：AI 解釋了複合函數的增減性其實就是狀態的 XOR 運算，這讓我秒殺了魔改後的 10055 題。
*   **精準度補償**：針對 10056 題，採用了 AI 建議的 `+ 1e-9` 小技巧，確保 `0.5454` 能正確進位到 `0.5455`。

### 3. 我拒絕的建議
*   **拒絕物理相減答案**：針對 10055 題，AI 一直堅持這是物理題。但我比對了教授的 `QUESTION-10055.md`，發現內容完全不對。我果斷拒絕 AI 的「標準答案」，要求它根據 .md 的新規則重寫。
*   **拒絕普通排序**：AI 建議用標準數字排序卡牌，但我堅持要按照大老二的規則 (2 > A > K...) 實作自定義 `__lt__`。

### 4. AI 錯誤修正案例
*   **Hash 遺漏**：AI 一開始給的大老二模型沒加 `__hash__`，導致測試崩潰。
*   **手動修正**：我查閱了 `game_design/p1-dev.md`，發現教授特別標註了雜湊值需求，於是我手動補齊了 `__hash__`。

---
**學號**：1112405041 | **姓名**：李易宸
